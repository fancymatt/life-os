import EntityBrowser from '../../components/entities/EntityBrowser'
import { qasConfig } from '../../components/entities/configs/qasConfig'

function QAsEntity() {
  return <EntityBrowser config={qasConfig} />
}

export default QAsEntity
